import { Route } from '@angular/router'

export const authRoutes: Route[] = [
  // { path: 'login', loadComponent: () => import('').then((c) => c.LoginComponent) },
  // { path: 'register', loadComponent: () => import('').then((c) => c.RegisterComponent) },
  // { path: '', redirectTo: 'login' },
  // { path: '**', redirectTo: 'login' },
]
